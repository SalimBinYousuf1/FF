# Android / Gradle build outputs
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
app/build/
/captures/
.externalNativeBuild/
.cxx/

# Sensitive files (never commit these)
keystore.properties
*.keystore
*.jks
!*.debug.keystore
google-services.json

# The wrapper JAR is NOT excluded — it must be committed
# !gradle/wrapper/gradle-wrapper.jar  ← this line intentionally absent

# Compiled outputs
*.class
*.dex
